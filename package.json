{
    "name": "learn_npm",
    "version": "1.0.0",
    "scripts": {
        "start": "echo \"Error: no start specified\" && exit 1",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "dayjs": "^1.11.13",
        "moment": "^2.30.1"
    },
    "devDependencies": {
        "nodemon": "^3.1.7"
    }
}
